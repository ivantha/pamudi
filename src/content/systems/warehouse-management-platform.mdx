---
# ⚠ PROVENANCE. Read before editing.
#
# The screens are Pamudi's own, exported 2026-09-03 from her Figma file (page
# "Acetrak Mobile (Collaborated)"). Every frame was checked before committing.
#
# The web console is NOT published and should not be added without a decision:
# its sidebar carries the product wordmark on every screen, and the product name
# is what `data/systems.yaml` anonymises to "Warehouse management platform". The
# mobile client carries no wordmark on any inner screen, which is why the six
# published here are all mobile.
#
# Demo data is synthetic (SKU_48484837, Item_48484837, bin 4-4-D) and the two
# personas in the sort list are plainly fictional, in the same category as the
# "John Doe" session label on the photo-ID screens.
#
# The prose is of two kinds and no third:
#   - Scope claims come from the 2025 appraisal record, via kb/projects.md
#     ("Acetrak WMS"). This was a system she maintained, not one she owned.
#   - Everything else describes what is visibly in the screens beside it.
#
# ⚠ The prose was rewritten on 2026-09-03 from the "Plinth" artboards in the
# Claude Design project, replacing the MDX body this file used to carry (it is
# in git, along with five further screens). Treat it the way data/site.yaml is
# treated: pending her sign-off.
system: Warehouse management platform
plate: 5
kind: extended
eyebrow: Third-party logistics · Inherited system
title:
  before: Warehouse
  accent: management
  after: " platform"
standfirst: >-
  An existing in-house WMS extended screen by screen. Her work was on the
  handheld picking flows, where the interface had to survive a scanner, a glove
  and a moving aisle.
meta:
  - label: Role
    value: Extending designer
  - label: Scope
    value: Picking and put-away flows
  - label: System
    value: In-house component set
index:
  summary: >-
    An existing in-house WMS extended screen by screen, on the handheld picking
    flows.
  shape: tall
  src: ../../assets/systems/warehouse-management-platform/batch-pick-items.png
  alt: Batch Pick item list on a pink ground, a SKU header reading 00 of 40 items scanned, three item cards each showing total item quantity with a Pick Qty field and a Pick button, and a Scan SKU ID field pinned at the foot.
figures:
  - layout: split-end
    number: Figure 1
    title: Batch pick
    note: Handheld
    shot: 15rem
    src: ../../assets/systems/warehouse-management-platform/batch-pick-items.png
    alt: Batch Pick item list on a pink ground, a SKU header reading 00 of 40 items scanned, three item cards each showing total item quantity with a Pick Qty field and a Pick button, and a Scan SKU ID field pinned at the foot.
    notes:
      - term: Scanner first
        text: >-
          The scan field keeps focus, so the device drives the screen and
          quantity is the only manual entry.
      - term: Progress pinned
        text: >-
          Items scanned against the batch total stays on the SKU group header
          while the item list scrolls.
      - term: Task-level state
        text: >-
          An exception tints the whole task rather than one row, so it survives
          a glance rather than a read.
argument:
  - label: Constraint
    lead: Extending, not replacing.
    paragraphs:
      - >-
        The platform was live and its component set was already in the build.
        New screens had to read as part of the same product, which meant working
        inside existing spacing, controls and colour rather than proposing a
        rebuild.
  - label: What changed
    lead: Fewer decisions per pick.
    paragraphs:
      - >-
        Item rows were rebuilt around the two numbers that matter — required
        quantity and picked quantity — with everything else demoted. Targets
        were raised to gloved-hand size and the scan field moved to the bottom
        bar, within thumb reach on a one-handed device.
related:
  - Manufacturing warehouse app
  - Mining-site tool platform
---
